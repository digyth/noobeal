<resources>

    <!-- Base application theme. -->
    <style name="Theme.Noobeal" parent="Theme.MaterialComponents.DayNight.NoActionBar">

        <item name="android:statusBarColor">@color/foreground_color</item>
        <item name="colorAccent">@color/foreground_color</item>
        <item name="colorPrimary">@color/foreground_color</item>
        <item name="colorPrimaryDark">@color/background_color</item>
    </style>

    <style name="Theme.Noobeal.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

</resources>